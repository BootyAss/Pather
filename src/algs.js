var Algs = {
    rand: (len) => { return _rand(len); },
    Kruskal: (x, y) => { return _Kraskal(len); }
};

var _rand = (len) => {
    return Math.round(Math.random() * len);
};

var _Kraskal = (x, y) => {
    let walls = new Set();
    return 1;
};

export { Algs };